1、重构功能
	将红外视频和可见光视频显示类分离，
	
	由于局域网，所以视频帧不需要缓存，红外显示 使用2个buff指针 一个用来存yousee发过来的视频帧，
	一个用来显示，交替使用。 可见光显示以红外的信号为准，更新矩形的时候更新视频帧（因为需要实时更新矩形框，所以需要以红外的帧率为基准）
	
	初始化参数获取（由qml统一发送请求）
	
	取消Xvideo中的定时器更新时间，由qml统一发送
	
	录像保存由avi保存 变为保存h264文件
	
